# Caches (mounted as volumes)
HF_HOME=/hf
HUGGINGFACE_HUB_CACHE=/hf/hub
TRANSFORMERS_CACHE=/hf/transformers

# Internal script paths (inside container)
SPLITTER_SCRIPT=/app/splitting/infer_splitter.py
STORYTELLER_SCRIPT=/app/storytelling/infer_from_splits.py

# Model adapters (inside container; mount host paths accordingly)
SPLITTER_ADAPTER_PATH=/doker/argese/clean_dataset/out-splitter-qwen7b/checkpoint-100
STORYTELLER_ADAPTER=/docker/argese/clean_dataset/qwen32b_storyteller_lora/final_best

# If your private API enforces a key; otherwise leave empty
API_KEY=
